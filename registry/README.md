# Registry

The registry is the source of truth for guide topic coverage and publication state.

- Canonical file: `editorial-guide-registry.md`
- Update this file immediately after a guide moves from queued work to dev DB or production DB state.
- Do not infer state from generated files. Use actual DB verification.
